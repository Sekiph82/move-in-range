# TESTING

Tests cover safety precedence, cardiac rehabilitation handling, duration totals, diabetes conversion, insight sample size, AI output safety, offline outbox, pain flow, and admin policy draft state. Backend pytest coverage is scaffolded for CI once Python is installed.
