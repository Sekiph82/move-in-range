# ARCHITECTURE

MoveInRange uses a monorepo with Expo mobile, Next.js admin, FastAPI API, shared deterministic TypeScript health engines, and SQLAlchemy/PostgreSQL persistence. Domain safety logic is isolated from UI and auth providers.
