# API

API routes are versioned under /api/v1 and include health, readiness, daily plans, exercise search, glucose logging, and admin policy/audit examples. Errors use structured detail payloads and correlation ID headers.
