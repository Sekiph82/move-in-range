# Contributing

Work on short-lived branches. Keep commits reviewable and named by intent. Run lint, type checking, tests, import validation, and migration validation before pushing.

Clinical, diabetes, medication, and contraindication rules require clinical review before publication. Draft rule changes must remain draft until an authorized clinical reviewer publishes a version.
